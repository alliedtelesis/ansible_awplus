---
- name: Debug task
  ansible.builtin.debug:
    msg: "START awplus_openflow reset config on connection={{ ansible_connection }}"

- name: Delete the current config
  alliedtelesis.awplus.awplus_openflow:
    config:
      controllers:
    state: overridden

- name: Debug task
  ansible.builtin.debug:
    msg: "END awplus_openflow reset config on connection={{ ansible_connection }}"
