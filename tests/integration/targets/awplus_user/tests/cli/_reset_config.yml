---
- name: Debug task
  ansible.builtin.debug:
    msg: "START awplus_user reset config on connection={{ ansible_connection }}"

- name: Delete the current config
  alliedtelesis.awplus.awplus_user:
    config:
    state: overridden


- name: Debug task
  ansible.builtin.debug:
    msg: "END awplus_user reset config on connection={{ ansible_connection }}"
