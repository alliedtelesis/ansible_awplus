---
- name: Debug task
  ansible.builtin.debug:
    msg: "START awplus_mlag reset config on connection={{ ansible_connection }}"

- name: Delete the current config
  alliedtelesis.awplus.awplus_mlag:
    config:
    state: overridden

- name: Debug task
  ansible.builtin.debug:
    msg: "END awplus_mlag reset config on connection={{ ansible_connection }}"
