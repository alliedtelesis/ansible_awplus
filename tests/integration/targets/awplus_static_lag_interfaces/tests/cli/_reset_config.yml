---
- name: Debug task
  ansible.builtin.debug:
    msg: "START awplus_static_lag_interfaces reset config on connection={{ ansible_connection }}"

- name: Delete the current config
  alliedtelesis.awplus.awplus_static_lag_interfaces:
    config:
    state: overridden

- name: Debug task
  ansible.builtin.debug:
    msg: "END awplus_static_lag_interfaces reset config on connection={{ ansible_connection }}"
