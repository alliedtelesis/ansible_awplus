---
- name: Debug task
  ansible.builtin.debug:
    msg: "START <module_name> replaced integration tests on connection={{ ansible_connection }}"

- block:
    - name: Initialise configuration
      ansible.builtin.include_tasks: _initial_config.yml

    - name: Replaced <task>
      alliedtelesis.awplus.<module_name>: &r1
        config:
        state: replaced
      register: result

    - name: Assert <task>
      ansible.builtin.assert:
        that:
          - result.changed == True

    - name: Replaced <task> (IDEMPOTENT)
      alliedtelesis.awplus.<module_name>: *r1
      register: result

    - name: Assert idempotency
      ansible.builtin.assert:
        that: 
          - result.changed == False

  always:
    - name: Reset configuration
      ansible.builtin.include_tasks: _reset_config.yml

- name: Debug task
  ansible.builtin.debug:
    msg: "END <module_name> replaced integration tests on connection={{ ansible_connection }}"